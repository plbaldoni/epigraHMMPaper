# Download and Process Input Data

* Run `download_encode.R` to download all the necessary ENCODE data
* Run `download_hg19.R` to download the reference genome and the black list of genomic coordinates
* Run `process_encode.R` to process the necessary ENCODE data
